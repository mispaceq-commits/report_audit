{
  "name": "report-audit",
  "version": "0.1.0",
  "private": true,
  "description": "Website Audit Report mockup pages for 48 Digital. HTML + Tailwind, exported to PDF/PNG via Puppeteer for use in Illustrator.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./src/styles.build.css --minify",
    "watch:css": "tailwindcss -i ./src/styles.css -o ./src/styles.build.css --watch",
    "serve": "http-server src -p 8080 -c-1",
    "export": "npm run build:css && node scripts/export.js",
    "build": "npm run build:css && npm run export"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "puppeteer-core": "^22.15.0",
    "tailwindcss": "^3.4.13"
  }
}
